export enum IdeaStatus {
  Pending = "pending",
  Roadmap = "roadmap",
  Startup = "startup",
  Business = "business",
  Rejected = "rejected",
  OnHold = "onHold",
}

export enum Requester {
  Owner = "owner",
  Other = "other",
}
