"use client";

interface IProps {
  filterRender?: (param: any) => void;
  clearFilter?: () => void;
  initData?: Record<string, unknown>;
}

const SupportiveCentersFilter: React.FC<IProps> = ({ filterRender, clearFilter, initData }) => {
  return <div>hhfjkdshhf</div>;
};

export default SupportiveCentersFilter;
