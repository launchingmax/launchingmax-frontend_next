import React from "react";

interface IProps {
  trigger?: React.ReactNode;
}

const StartupFilter: React.FC<IProps> = ({ trigger }) => {
  return <h2>StartupFilter</h2>;
};

export default StartupFilter;
